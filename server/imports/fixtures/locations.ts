
import { Locations } from '../../../both/collections/locations.collection';
import { Location } from '../../../both/interfaces/location.interface';

export function loadLocations() {

    // if (Locations.find().count() === 0) {
    //     const locations: Location[] = [
    //     {name: "Blue Hills 2", shortName: "BH2", region: "Nassau", country: "Bahamas"},
    //     {name: "Northside Waterworks", shortName:  "NSWW", region: "West End - Grand Cayman", country: "Cayman Islands"}
    // ];

    // locations.forEach((location) => Locations.insert(location));

    // }

}